namespace Beltek.DizilerApp
{
    internal class Program
    {
        static void Main(string[] args)
        {
            /*int sayi = 5;*///stack
                             //diziler-Array

            //int[] sayilar = new int[3];//heap bölgesinde tutulur
            //sayilar[0] = 10;
            //sayilar[1] = 20;
            //sayilar[2] = 30;

            //Console.WriteLine(sayilar[2]);

            //int[] numbers = { 10, 20, 30 };

            //Array.Sort(numbers);

            //for (int i = 0; i < numbers.Length; i++)
            //{
            //    Console.WriteLine(numbers[i]);
            //}

            //Console.WriteLine("kaç adet sayı gireceksiniz?");
            //int adet = int.Parse(Console.ReadLine());
            //int[] sayilar = new int[adet];

            //for (int i = 0; i < adet; i++)
            //{
            //    Console.WriteLine("sayı giriniz:");
            //    sayilar[i]=int.Parse(Console.ReadLine());

            //}
            //Array.Sort(sayilar);
            //Console.WriteLine("sıralı hali:");

            //for (int i = 0;i < adet; i++) {
            //    Console.Write($"{sayilar[i]} ");//sayıları yan yana yazdırma
            //}

            //foreach (var sayi in sayilar)
            //{
            //    Console.Write($"{sayi} ");
            //}

            int count = 0;
            while (count < 5)
            {
                count++;
            }

            int i = 0;
            for (; i < 5;)
            {
                i++;
            }

            do
            {

            } while (false);//çalışmaz




            //3 elemanlı dizi    

            //birden fazla veriyi aynı anda sıralama için kullanılır.
            //index (sıfırdan başlar) her elemanı temsil eden numaralar
            //sayilar dizisi içindeki her bir int sayi kadar dön -foreach döngüsü
        }
    }
}